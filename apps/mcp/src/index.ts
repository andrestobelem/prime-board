#!/usr/bin/env bun
// MCP server con tools espejo del MCP de Linear. Se implementa en AT-142.
import { APP_VERSION } from "@prime-board/schema";

console.error(`pb-mcp ${APP_VERSION} — prime-board MCP server (not implemented yet)`);
